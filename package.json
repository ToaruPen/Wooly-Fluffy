{
  "name": "wooly-fluffy",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "typecheck": "npm run -w server typecheck",
    "lint": "npm run -w server lint",
    "test": "npm run -w server test",
    "coverage": "npm run -w server coverage",
    "deadcode": "knip"
  },
  "devDependencies": {
    "knip": "^5.80.2"
  }
}
