# 未確定事項（次の議論の入口）

このファイルは、まだ決めきれていない点・次の議論の入口です（優先度順）。

## 1) 運用/安全（優先）

- STAFF UIのアクセス制御（方針は「別端末ブラウザ（OS不問）」。残り：パスコード/自動ロック/LAN内限定のやり方を決める）
- 緊急停止（ミュート/停止）の運用（UIでできること、物理側で担保すること）
- 同名が出た時の現場運用（ニックネームの作り方、改名手順）

## 2) `ROOM` の「みんなの思い出」

- 作り方: 職員手入力のみ / 半自動（職員がキーワード入力→文章化）/ 自動抽出（リスク高め）
- 保持期間: 年度/学期単位で棚卸しするか、期限を決めるか

## 3) データ保持/削除

- `ROOM` の短期セッション保持時間（RAMで何分持つか。ディスク保存はしない）
- 削除要求への対応手順（誰が、どの画面で、何を消すか）
- バックアップ/復旧（正はローカルSQLite。`confirmed`/`ROOM` を暗号化してクラウド退避する場合の保持期間・削除手順。Time Machine等との整合も含む）

## 4) 技術スタック選定（探索項目）

- UI/Backend: ローカルWeb（React + Node/TypeScript）を採用予定。Electron/Tauri/Unity等はMVP後に必要性を再検討
- STT: クラウドWhisper系 / ローカルwhisper.cpp / その他OSS の比較（騒音環境・遅延・運用）
- TTS: speechSynthesis / VOICEVOX等 の採否（ライセンス/クレジット運用含む）
- LLM: クラウド vs ローカル（Ollama/llama.cpp等）と、ネット断時のフォールバック設計

## 5) 将来: NFC導入（B）

- 別の子がタッチした場合の挙動（拒否/切替/キュー/職員介入）
- 「覚えていい？」の確定をNFC再タッチに戻すか、職員Confirmを残すか
- NFC UIDの扱い（ハッシュ化、タグ再発行、紛失対応）

## 6) ハード/現場

- マイク/スピーカー構成（机上デバイス vs ぬいぐるみ内部への組み込み範囲）
- モニター常設の kiosk 運用（自動起動、フルスクリーン、復旧手順）
- ぬいぐるみ筐体へ移行する場合のI/O（ボタン/NFC/スピーカー/マイク）の取り回し

## 7) 将来機能

- 個別呼びかけ（リマインド/スケジュール）を入れるタイミングと安全設計
- ゲーム（オセロ等）の実装方式と「結果の記憶」の扱い
- Webカメラ利用（保存しない/個人識別しない）で、どのシグナルを使うか（人がいる/人数/表情の粗い分類）。カテゴリ設計・更新頻度・運用掲示（カメラ使用の告知）を決める
- KIOSKアバター表現（Live2D / 3D）で、採用するSDK/モデル形式（例: VRM）と利用条件（配布/クレジット/改変可否）の扱いを決める
