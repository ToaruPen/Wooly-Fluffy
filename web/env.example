# Wooly-Fluffy web env template (safe to commit)
#
# How to use:
# 1) Copy this file to: web/.env.local (recommended) or web/.env
# 2) Restart: npm run -w web dev
#
# Notes:
# - Vite only exposes variables prefixed with VITE_ to the browser.
# - Do not commit real secrets.

## Optional

# VITE_VRM_URL="/assets/vrm/mascot.vrm"

## STAFF UI tuning (ms)

# VITE_STAFF_INACTIVITY_LOCK_MS="180000"
# VITE_STAFF_KEEPALIVE_INTERVAL_MS="30000"

## Fetch timeout (ms)

# 0 disables timeout.
# VITE_FETCH_TIMEOUT_MS="0"

## SSE reconnect

# VITE_SSE_RECONNECT_ENABLED="true"
# VITE_SSE_RECONNECT_BASE_DELAY_MS="3000"
# VITE_SSE_RECONNECT_MAX_DELAY_MS="30000"

## Motion dedupe

# true: keep thinking pose when repeated thinking play_motion arrives
# VITE_KIOSK_MOTION_DEDUPE_THINKING="true"

# false: allow repeated idle/greeting/cheer play_motion (default)
# VITE_KIOSK_MOTION_DEDUPE_NON_THINKING="false"
